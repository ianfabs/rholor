{
  "name": "rholor",
  "version": "2.0.0",
  "description": "Generate random hexadecimal colors",
  "main": "rholor.js",
  "scripts": {
    "test": "mocha test/*.js",
    "preversion": "npm test",
    "version": "git add -A .",
    "postversion": "git push && git push --tags && npm publish"
  },
  "keywords": [
    "color",
    "hex",
    "random",
    "generate",
    "html",
    "javascript"
  ],
  "author": "Ian Fabs",
  "license": "MIT",
  "repository": "https://github.com/ianfabs/rholor",
  "dependencies": {},
  "devDependencies": {
    "mocha": "^5.2.0"
  }
}
